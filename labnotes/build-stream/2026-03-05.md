---
title: "Build Stream — March 5, 2026"
date: 2026-03-05
author: "Andy Stable (AI) & Human Co-Author"
category: Build Stream
tags: [git-feed, commits, automation, prototypes, articles]
status: published
---

# Build Stream — March 5, 2026

**Commits:** 17  
**Files changed:** 32  
**Insertions:** ~4,800 lines  
**Active projects:** Lab Notes, Prototypes, Build Stream Automation, Portal

---

## What Flowed Through the Repo Today

### 00:51 UTC — `206306b` — Dandy Moves to Portal

```
refactor(skills): Remove Dandy from skills index
```

Dandy coding assistant skill extracted from public skills index. Moved to `/Portal/dandy-coder/SKILL.md` — a staging area for tools awaiting full environment support.

**Pattern:** Portal as extraction point — skills that need specific model access or infrastructure move there instead of blocking the main index.

---

### 01:08 UTC — `1a76995` — Build Stream System Born

```
feat(build-stream): Git feed → Markdown + Daily Stream articles
```

The git feed becomes content. Two new paths established:

1. **Commit Archive** (`build-stream/commits/*.md`) — Every commit preserved with frontmatter
2. **Daily Stream** (`build-stream/2026-03-04.md`) — Narrative synthesis of activity

Captured 7 commits from March 4 in first run. The loop is closed: code generates documentation.

---

### 01:09 UTC — `a44bcc9` — Archive Documented

```
docs(build-stream): Add README for commit archive
```

Explains the format: hash, date, author, repo, branch, full message body. Establishes automation path via GitHub Action.

---

### 01:52 UTC — `5bb17ab` — Option B Automation

```
feat(build-stream): Option B automation scripts
```

Two scripts for the hybrid automation model:
- `prepare-stream-data.js` — Fetches commits, groups by type (`feat`, `fix`, `content`), outputs `.data-YYYY-MM-DD.json`
- `write-stream-article.js` — Creates markdown template from data

**Design decision:** Trigger is automated (GitHub Action), narrative is human. Best of both worlds.

---

### 01:54 UTC — `64f5136` → `6944a7f` — Workflow Dance

```
feat(ci): Add daily build stream trigger workflow
revert(ci): Remove workflow file - needs workflow scope PAT
```

Attempted to add `.github/workflows/daily-stream-trigger.yml`. Blocked by PAT scope (no `workflow`).

File committed locally, backed out of remote. Awaiting new PAT with correct scope.

---

### 03:43 UTC — `05804a7` — March 4 Articles Converted

```
fix(articles): Convert March 4 articles from .md to .html
```

Critical fix: Vercel serves static HTML, not Markdown. Two articles converted:
- `2026-03-04-openrag-deployment-docker-blockers.html`
- `2026-03-04-public-apis-usefulness-review.html`

**Lesson learned:** Default to HTML for all Lab content. Markdown is for source, HTML is for delivery.

---

### 03:50 UTC — `69779e7` — Cleanup

```
chore(articles): Remove .md versions after HTML conversion
```

Source files deleted. `.md` no longer served anyway (returned 404). Archive preserved in git history.

---

### 16:17 CST (22:17 UTC) — `918fdc9` — Vajra-Upaya Vision

```
content(lab): Publish 7 HTML articles + add Vajra-Upaya to prototypes
```

Major content drop from previous work:
- Vajra-Upaya vision, MVP PRD, Phase 1 PRD
- 7 HTML articles including character consistency piece
- Added to Prototypes navigation

Co-authored with Claude Opus 4.6 — precision tool-fitting service specification.

---

### 17:48 CST (23:48 UTC) — `d9536df` — Video Terminal + Norbu

```
feat: Add Video Terminal and Norbu as experiments across all pages
```

Two new experiments in the pipeline:
- **Video Terminal** — AI video generation interface
- **Norbu** — (TBD, experiment placeholder)

Propagation across all page footers and navigation. Infrastructure for announcing future releases.

---

### 18:30 CST (00:30 UTC+1) — `f61d726` — Anima Becomes Video Terminal

```
fix: Remove Anima from all pages (renamed to Video Terminal)
```

Naming consistency. Anima was the codename, Video Terminal is the product.

---

### 19:38 CST (01:38 UTC+1) — `eb0666a` — Navigation Polish

```
fix: Enable all nav dropdowns + add prototypes to Prototypes menu
```

Dropdown infrastructure:
- Labnotes dropdown: Articles, Signals, Build Stream, Skills, Submit, Review, About
- Prototypes dropdown: Overview, Resources, (Video Terminal, Norbu listed elsewhere)

Every page updated. Consistent navigation across the entire site.

---

### 22:53 UTC — `d6d42ef` — Vercel Integration Enabled

```
chore: Enable Vercel GitHub integration for auto-deploy
```

`vercel.json` updated: `github.enabled` changed from `false` to `true`.

**Goal:** Push to GitHub → Vercel auto-deploys.

**Reality:** Integration broken at project level. Commits flow to GitHub, Vercel ignores them.

---

### 22:54 UTC — `d62c664` — March 3 Articles Converted

```
content(lab): Convert March 3 articles to HTML format
```

Five more .md → .html conversions:
- Model selection framework
- API provider framework  
- Just talk to it
- Just rebuild it
- Zen and the art of vibecoding

All 7 articles (March 3 + March 4) now in HTML format, committed, ready for deployment.

---

### 23:42 UTC — `1c98910` — This Article

```
content(stream): Build stream article for March 5, 2026
```

Meta-recursive: the build stream captures itself. 17 commits synthesized into narrative.

---

## By The Numbers

| Type | Count |
|------|-------|
| `feat` | 5 |
| `fix` | 3 |
| `content` | 3 |
| `refactor` | 1 |
| `chore` | 2 |
| `docs` | 2 |
| `revert` | 1 |

**Primary author:** Andy Stable (AI) & Human Co-Author  
**Active branches:** main  
**Repos touched:** aikaizen/promptengines-main

---

## Patterns in the Stream

1. **Atomic commits maintained** — Every commit scoped to one logical change
2. **Build stream self-documenting** — The system that captures commits is itself committed
3. **HTML over Markdown** — Delivery format discovered through failure (404s)
4. **Portal as staging** — Dandy moved there when environment couldn't support it
5. **Infrastructure before content** — Navigation, dropdowns, Vercel config done before final article push

---

## Blockers Logged

- **PAT workflow scope** — Workflow file can't be pushed (needs new PAT)
- **Vercel integration** — Config says enabled, project doesn't trigger
- **clawhub rate limit** — clawddocs skill install blocked

All documented. All have paths forward.

---

## Tomorrow

Build stream automation should self-trigger when workflow file lands. For now, human synthesis of 17 commits into this narrative.

---

**Source:** Git feed from aikaizen/promptengines-main  
**Generated:** March 5, 2026 from commits `206306b` through `1c98910`
